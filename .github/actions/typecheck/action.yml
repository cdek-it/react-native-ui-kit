name: Run Typechecks
description: Typecheck the project and validate generated tokens

runs:
  using: composite
  steps:
    - uses: ./.github/actions/setup-node

    - name: Run Typecheck
      shell: bash
      run: yarn tsc --noEmit

    - name: Validate Design Tokens
      shell: bash
      run: yarn tokens:validate
